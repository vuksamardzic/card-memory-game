import '../scss/main.scss';

const arr = [1, 2, 3];
const el = document.querySelector('body');


arr.forEach((i, index) => {
  // const n = document.createElement('p');
  // n.append(`${i} - el`);
  // el.appendChild(n);
});
